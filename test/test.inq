var a = 3, b = 7;

function test(a: number) {
    turnLeft(a);
}

on whenGreenFlag {
    var b = 5;
    say(a + b);
    forward(10);
    think("that.");
}

on whenKeyPressed('space') {
    say("This is space!");
    doAsk("How are you today?");
    say(answer);
    for(var i = 0; i < 10; i++) {
        say(i);
        forward(i+1);
    }
}